Create a variable called z, assign x + y to it, and display the result.

    package main
    
    import ("fmt")
    
    func main() {
        var x = 5
        var y = 10
        var z = x + y
        fmt.Println(z)
    }
