Use the correct arithmetic operator to increase the value of the variable x by 1.

    package main
    
    import "fmt"
    
    func main() {
        x := 1
        x += 1
    
        fmt.Print(x)
    }
