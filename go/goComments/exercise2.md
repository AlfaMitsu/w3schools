Multi line comments in Go are written with a special character, which one?

    package main
    import ("fmt")
    
    func main() {
      
    // These lines
      are comments // 
    
      fmt.Println("Hello World!")
    }
