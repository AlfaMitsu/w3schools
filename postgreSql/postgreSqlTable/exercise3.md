Complete the SQL statement to include a text field called "color" with maximum 10 characters.

    CREATE TABLE cars (
      brand VARCHAR(255),
      model VARCHAR(255),
      year INT,
      color VARCHAR(10)
    );
