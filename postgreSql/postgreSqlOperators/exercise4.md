Complete the select statement so that it returns all cars with either the brand 'Ford' or the brand 'Volvo'.

Hint: Use the logical OR operator

    SELECT * FROM cars
    WHERE brand = 'Ford' OR brand = 'Volvo';
